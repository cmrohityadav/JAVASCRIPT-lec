/*

== : abstract equality operator
    it checks the type of both operands
    if type is same ,then it calls ===
    if the types are not same then type conversion occurs (coercion) an then comapre 
    is done



=== : strict equality operator
    it checks the type of both operands
    if types are different it return false
    if types are same then value comparision happen

*/


console.log(1=="1");
console.log(1=="rohit");


